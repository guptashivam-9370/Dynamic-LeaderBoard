/**
 * Leaderboard Tab Screen
 * 
 * This is the entry point for the Leaderboard tab.
 * It renders the LeaderboardScreen component.
 */

import { LeaderboardScreen } from '@/screens';

export default function LeaderboardTab() {
  return <LeaderboardScreen />;
}
