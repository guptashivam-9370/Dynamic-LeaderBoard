/**
 * Search Tab Screen
 * 
 * This is the entry point for the Search tab.
 * It renders the SearchScreen component.
 */

import { SearchScreen } from '@/screens';

export default function SearchTab() {
  return <SearchScreen />;
}
